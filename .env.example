# Frontend env vars (prefix with VITE_ to expose to the browser)
# Copy this to .env.local for local dev, or set in your hosting provider's dashboard

# URL of the running FastAPI backend
VITE_API_URL=http://localhost:8000
